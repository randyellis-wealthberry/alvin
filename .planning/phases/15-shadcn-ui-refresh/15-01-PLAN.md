---
phase: 15-shadcn-ui-refresh
plan: 01
type: execute
depends_on: []
files_modified: [src/styles/globals.css, src/components/ui/button.tsx, src/components/ui/input.tsx, src/components/ui/label.tsx, src/components/ui/card.tsx]
---

<objective>
Set up Shadcn CSS theme variables and create core UI components.

Purpose: Establish the design system foundation that all other components will build upon.
Output: globals.css with Shadcn theme, 4 core UI components ready for use.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference implementation:
@design-os/src/index.css
@design-os/src/components/ui/button.tsx
@design-os/src/components/ui/input.tsx
@design-os/src/components/ui/label.tsx
@design-os/src/components/ui/card.tsx

# Existing project files:
@src/styles/globals.css
@src/lib/utils.ts
@components.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Shadcn CSS variables to globals.css</name>
  <files>src/styles/globals.css</files>
  <action>
Replace the minimal globals.css with Shadcn theme variables adapted from design-os/src/index.css.

Include:
1. Import `tw-animate-css` (npm install first if not present)
2. @theme inline block with all CSS variable mappings (radius, colors)
3. :root light theme variables (background, foreground, card, primary, secondary, muted, accent, destructive, border, input, ring)
4. .dark class dark theme variables
5. @layer base styles for borders and body

Key adaptations:
- Keep the warm stone palette from design-os (professional, not garish purple)
- Use oklch color format for modern browsers
- Keep --radius: 0.625rem for consistent rounded corners

Do NOT copy sidebar-specific variables (sidebar-primary, etc.) - not needed for this app.
  </action>
  <verify>npm run build succeeds with no CSS errors</verify>
  <done>globals.css contains Shadcn theme variables, light/dark mode support</done>
</task>

<task type="auto">
  <name>Task 2: Create core UI components (Button, Input, Label, Card)</name>
  <files>src/components/ui/button.tsx, src/components/ui/input.tsx, src/components/ui/label.tsx, src/components/ui/card.tsx</files>
  <action>
Create `src/components/ui/` directory and copy components from design-os, adapting imports:

**button.tsx:**
- Copy from design-os/src/components/ui/button.tsx
- Change `import { cn } from "@/lib/utils"` to `import { cn } from "~/lib/utils"`
- Keep all variants: default, destructive, outline, secondary, ghost, link
- Keep all sizes: default, sm, lg, icon, icon-sm, icon-lg

**input.tsx:**
- Copy from design-os/src/components/ui/input.tsx
- Adapt import path to ~/lib/utils

**label.tsx:**
- Copy from design-os/src/components/ui/label.tsx
- Adapt import path
- Note: uses @radix-ui/react-label - need to install

**card.tsx:**
- Copy from design-os/src/components/ui/card.tsx
- Adapt import path
- Exports: Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent

Run `npm install @radix-ui/react-slot @radix-ui/react-label` before creating components.
  </action>
  <verify>npm run typecheck passes with new components</verify>
  <done>4 components exist in src/components/ui/, all export correctly, typecheck passes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] `npm run typecheck` passes
- [ ] src/components/ui/ contains button.tsx, input.tsx, label.tsx, card.tsx
- [ ] globals.css contains :root and .dark theme variables
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- CSS variables properly defined for light/dark themes
- Core components exported and ready for use
</success_criteria>

<output>
After completion, create `.planning/phases/15-shadcn-ui-refresh/15-01-SUMMARY.md`
</output>
