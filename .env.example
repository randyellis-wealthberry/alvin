# ============================================================================
# ALVIN Environment Variables
# ============================================================================
# Copy this file to .env.local for local development
# For Vercel deployment, add these in the Vercel dashboard (Settings > Environment Variables)

# ============================================================================
# DATABASE (Required) - PostgreSQL
# ============================================================================
# Vercel Postgres (recommended) - auto-populated when you run `vercel link`
# Or use any PostgreSQL provider: Supabase, Neon, Railway, etc.
DATABASE_URL="postgres://user:password@host:5432/database?sslmode=require"

# Direct connection for Prisma migrations (bypasses Vercel's connection pooling)
# Only needed for Vercel Postgres - same as DATABASE_URL but without pooling
DATABASE_URL_UNPOOLED="postgres://user:password@host:5432/database?sslmode=require"

# For local development with local PostgreSQL:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/alvin"

# ============================================================================
# AUTHENTICATION (Required)
# ============================================================================
# Generate with: openssl rand -base64 32
AUTH_SECRET="your-secret-key-here"

# ============================================================================
# AI (Required)
# ============================================================================
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY="your-anthropic-api-key"

# ============================================================================
# CRON JOBS (Required)
# ============================================================================
# Secret to secure cron endpoints - generate with: openssl rand -base64 32
CRON_SECRET="your-cron-secret-key"

# ============================================================================
# WEB PUSH NOTIFICATIONS (Required)
# ============================================================================
# Generate VAPID keys with: npx web-push generate-vapid-keys --json
VAPID_PRIVATE_KEY="your-vapid-private-key"
VAPID_CONTACT_EMAIL="support@example.com"
NEXT_PUBLIC_VAPID_PUBLIC_KEY="your-vapid-public-key"

# ============================================================================
# EMAIL (Optional - Required for email features)
# ============================================================================
# Get from: https://resend.com/api-keys
# RESEND_API_KEY="your-resend-api-key"

# ============================================================================
# SMS (Optional - Required for SMS features)
# ============================================================================
# Get from: https://console.twilio.com
# TWILIO_ACCOUNT_SID="your-twilio-account-sid"
# TWILIO_AUTH_TOKEN="your-twilio-auth-token"
# TWILIO_PHONE_NUMBER="+1234567890"

# ============================================================================
# CONVEX (Optional - If using Convex for real-time features)
# ============================================================================
# NEXT_PUBLIC_CONVEX_URL="your-convex-deployment-url"
